import { ReactNodeList } from "@/interface";
import FiberRoot from "./FiberRoot";

/**
 * 更新容器
 * @param element children
 * @param conatiner 容器
 * @param parentComponent 父组件，在首次render时为null
 */
export function updateContainer(element: ReactNodeList, conatiner:FiberRoot, parentComponent: null) {
  // request eventTime 

  // request lane

  // 创建update


}